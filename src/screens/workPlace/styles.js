import { colors } from '../../config';

export default {
    container: {
        backgroundColor: colors.blue,
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    }
};