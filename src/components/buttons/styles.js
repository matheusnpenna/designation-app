import { colors } from '../../config';

export default {
    largeButtonContainer: {
        backgroundColor: colors.darkGrey,
        width: '100%',
        padding: 20,
        borderRadius: 15,
        alignItems: 'center',
        justifyContent: 'center'
    },
    label: {
        color: colors.white
    }
};